[package]
name = "wizard"
version = "0.1.4"
edition = "2024"
description = "A fast, fuzzy-aware alternative to which"
license = "MIT"

[dependencies]
rayon = "1.12.0"
unicode-segmentation = "1.13.3"

[profile.release]
lto = "thin"
codegen-units = 1
strip = true

